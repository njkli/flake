# TODO: chromium-browser-master-preferences.patch
final: prev: {
  # chromium.browser = prev.chromium.browser.overrideAttrs (oldAttrs: {
  #   patches = oldAttrs.patches ++ [ ./chromium-browser-master-preferences.patch ];
  # });
}
